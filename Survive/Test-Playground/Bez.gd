extends Polygon2D


# Declare member variables here. Examples:
# var a: int = 2
# var b: String = "text"

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	#print(self.polygon[0])
#	var p0 : Vector2 = self.polygon[0]
#	var p1 : Vector2 = self.polygon[1]
#	var p2 : Vector2 = self.polygon[2]
#	print(self.polygon)
#
#	var q0 = p0.linear_interpolate(p1, .2)
#	var q1 = p1.linear_interpolate(p2, .2)
#	var p = q0.linear_interpolate(q1, .2)
#
#	print(p)
#	self.polygon.insert(1, Vector2(200,200))
#	self.polygon
#	print(self.internal_vertex_count)
	
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta: float) -> void:
#	pass
